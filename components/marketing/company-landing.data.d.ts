// Types for the auto-generated company-landing.data.js (allowJs is off, so the
// .js module needs an ambient declaration to be importable from TS).
export const CSS: string;
export const BODY: string;
export function initCompanyLanding(): void;
